# Prompt log — 2026-07-20

- Báo giá sale ký gửi: wire lại API estimate (ưu tiên cước local đơn giá × cân), dùng `pricingRuleIds` khách chọn để bật phụ phí; luồng khách chọn dịch vụ → Sales tính phí.
- Đối chiếu swagger BE: gỡ gọi API đã chết (`/quotation/estimate`, `/api/Operations/*`, `GET /purchase-orders/{id}`); căn `AdditionalFeeDto` theo swagger.
- Báo giá: bỏ checkbox bật/tắt phụ phí và UI thêm phí tùy chỉnh — chỉ hiện phí khách đã chọn, Sales chỉnh số lượng.
- Lệch tổng chi tiết vs lập báo giá: BE snapshot gộp serviceFee/taxAndDuty; bỏ ép kiểm hàng theo requiresInspection; merge snapshot sau send.
- Hiển thị thời điểm kèm UTC±X theo múi giờ máy người dùng (`src/utils/dateTime.js`).
- Chỉnh ngữ nghĩa mục Thông số đơn hàng & cước chính (cân thực / DIM / cân tính phí; bỏ copy bật-tắt phụ phí).

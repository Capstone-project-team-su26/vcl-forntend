# Prompt log — 2026-07-21

- Validate tạo user admin: giữ `region` theo quy ước nhóm; bổ sung validation form (họ tên/email/SĐT/mật khẩu), check trùng email & SĐT trên danh sách hiện có (+ mock 409), chỉnh ngữ nghĩa copy/hint vai trò và field.
- Admin users: bỏ role Delivery; tách nav Nhân viên nội bộ vs Tài khoản khách; bỏ quản lý khách bên Sale (redirect + xóa CRM pages).
- Admin Quản lý khách: thêm tab Hồ sơ khách (list/detail/sửa) + tab Tài khoản đăng nhập.
- Ghép hồ sơ ↔ TK khách theo email rồi SĐT; hiện cột liên kết + khóa TK ngay trên chi tiết hồ sơ.
- Bỏ 2 tab Quản lý khách — một list hồ sơ (cột TK); khóa TK trên chi tiết.
- Thông báo tài khoản: ActionNotice (tự tắt) + xác nhận khóa/mở cho TK khách & nhân viên.
- Admin kho: bỏ Zone/Shelf/Bin; chỉ list/tạo/sửa kho + sức chứa (BE swagger chưa có capacity — mock lưu, API bỏ qua field).
- Audit kho DB: pipeline check-in → put-away → inventory lệch; FE sửa phiếu tiếp nhận chỉ kho Destination, canonicalize warehouseType, label status check-in (chưa = tồn).
- Admin bỏ capacity (ảo); Ops thêm trang Phân bố vị trí kho (tạo Zone/Shelf/Bin + gắn layout grid, dùng API locations/layout sẵn có).
- Handoff TODO BE + Warehouse Staff App: `doc/warehouse-ops-handoff.md`.
- Login/API: map lỗi cryptic (`error code: 502`, Bad Gateway…) trong `getErrorMessage` sang tiếng Việt dễ hiểu.
- Toast kết quả hành động: flash toast sống qua redirect; login/logout thông báo thành công (và lỗi login).
- Sale tạo ký gửi: bổ sung parity web khách (người nhận, DxRxC, thùng, tracking, ảnh URL); mapper gửi đủ field; BE default status staff create → PENDING_REVIEW (không còn tự APPROVED).
- Sale tạo ký gửi: làm lại theo layout web khách (`/create-order/consignment`) — tách tuyến/hình thức, multi-kiện, dropdown product-types, upload ảnh `/api/uploads/images`.
- Fix upload ảnh: proxy Next đọc multipart bằng arrayBuffer (trước dùng text làm hỏng file); fallback `/api/uploads/image` vì BE chưa chắc có batch.

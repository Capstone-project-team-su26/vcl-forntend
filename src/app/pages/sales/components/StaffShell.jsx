/**
 * @deprecated Shell đã chuyển lên `sales/layout.jsx` (SalesLayoutShell).
 * Giữ file để tránh import cũ gây lỗi build — chỉ render children.
 */
export default function StaffShell({ children }) {
  return children;
}
